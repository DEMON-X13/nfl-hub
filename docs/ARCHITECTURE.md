# The map

Deeper reference than `CLAUDE.md`. Read that first for the build loops and the
working agreement; read this when you need to find something or need the
vocabulary. It is a map, not a copy of the code -- the code is the truth, and
anything here that contradicts it is out of date.

## What this is

One repo, three independent single-page apps, served straight from the repo root
by GitHub Pages. No server, no database, no framework, no bundler. Each app is
one big self-contained HTML file with its data baked in; scheduled GitHub
Actions rebuild them from free data sources and commit the result.

`index.html` at the root is a static landing page linking the three.

```
betting/   X NFL Betting Model    game picks, win probabilities, power ratings
props/     Prop Model             player props, correlated parlays
news/      Season Tracker         newsletter-style week-by-week site
```

Everything a visitor does -- picks, parlays, bankroll, bets, odds they load --
lives in their own browser's local storage and never reaches the repo. Only a
commit changes what anyone else sees.

## Repo layout

```
CLAUDE.md               build loops, conventions, how the owner works
docs/ARCHITECTURE.md    this file
index.html              landing page

betting/
  app/x_nfl_betting_model.html   THE SOURCE. one file, ~300KB, copied in from
                                 the nfl-model-lab repo when a version ships
  state.json                     generated by tools/update.js -- the season
  events.json                    hand-written: team news the job cannot infer
  joker.json                     the Joker model's per-game picks
  joker/                         the Joker: python, fitted model, research harness
  tools/update.js                download + grade + write state.json
  tools/build.js                 the app, built: buildApp() for nflbets/build/build.js;
                                 run on its own it checks and writes nothing
  tools/smoke.js                 loads the built app, plain and embedded, and checks it
  tools/reference_models.json    the release gate's expected model numbers

props/
  build/part1.html               THE SOURCE: <head>, all CSS, page skeleton
  build/part2.js                 THE SOURCE: state, model maths, odds, parlay maths
  build/part3.js                 THE SOURCE: rendering, wiring, the parlay engines
  build/assemble.py              part1 + payload.json + part2 + part3 -> the app
  build/audit.js                 ~25k checks. THE COMMIT GATE
  build/weekly.py                the whole refresh: download, price, bake, assemble,
                                 audit, commit
  build/patch_*.py               one committed script per past change, each with a
                                 prose docstring saying why
  data/payload.json              generated. everything baked into the page
  data/*.csv, *.json             prices, lines, fitted model pieces
  app/prop_model_2026.html       generated by assemble.py, gitignored: the audit's
                                 subject, never published
  raw/feat.pkl                   43MB fitted feature table, committed so the job
                                 does not rebuild it

elo/
  build.py                       THE SOURCE: the player Elo formula and the roster model,
                                 explained in its docstring; downloads 2012-now nflverse
                                 player stats into cache/ (gitignored) and writes data/
  data/players.json              generated: rankings by position, every rated player, the
                                 season-end top tens since 2012
  data/model.json                generated: the fitted position weights (overall and by
                                 season), the walk-forward record, this season's graded
                                 calls and the coming week's
  data/matchups.json             generated: the matchup formula per position and stat, its
                                 walk-forward record, the coming week's projections
  requirements.txt               pandas, numpy

news/
  index.html  js/app.js  css/    the site, hand-maintained
  data/weeks.js                  the list of published weeks; the page shows the last
  data/weekN.js                  one week each. DRAFTED by the job, NARRATIVE BY A PERSON
  data/results.js  stats2026.js  generated by tools/pull-week.js
  data/teams.js                  hand-written team reference
  tools/pull-week.js             refreshes results and stats, drafts weekN.js
  tools/run-auto.js              works out the current week, then runs pull-week
  HANDOFF.md                     how to publish a week -- read before touching news/
```

## Bets and Stats: one page over both models

`nflbets/` (X NFL Bets and Stats) is the one site: the props and betting sites have no pages
of their own any more, only their source, tools, jobs and data. `nflbets/build/build.js` assembles the prop model
from its parts (title, heading and tab bar swapped; every prop model section kept in the
page, listeners and all, whether or not it has a button yet), sets the Pick'ems board in
front of it from `nflbets/build/tab_pickems.html` (its own section, styles and closure,
every class and id prefixed `pk-`), and sets the betting app's Records, Power Ratings
and Bet Log tabs into frames: the app as `betting/tools/build.js` builds it is carried in
the page as a string and becomes a frame's srcdoc when its tab is first opened, with
`window.EMBED_TAB` (the tab, which also hides the app's header and tab bar) and
`window.STATE_URL` (`../betting/state.json`) written in front of it. A srcdoc frame is the
page's own origin, so the app keeps its browser store, and nothing is fetched or cached
for it apart from the page. The board's tag and confidence bands are lifted from the betting app at
build time and scoped inside the closure, since the prop model has its own `tag()`.
The prop model's tabs run on the prop model's own state under its own storage key, and
the framed tabs on the betting app's, so a pick, parlay or bet made in either is what
this page shows. Tabs are addresses: `#slate`, `#parlay`, `#record` and so on.
The Player Elo tab (`nflbets/build/tab_elo.html`, `pe-` prefixed, its own closure) reads
`elo/data/players.json` and `elo/data/model.json` on load and draws the coming week's calls
from the roster model beside the betting model's, the season's graded calls, the rankings
by position with each player's season line, the season-end top tens since 2012, and the
fitted weight of every position, overall and season by season. It borrows the Pick'ems
tab's team tag through `window.pkTag`.
Live Parlays is a section of the Parlay Builders tab, standing where the prop model's
Saved parlays card is: `nflbets/build/build.js` lifts `liveparlays/build/page.html` -- its
styles scoped to `#lpCard` and its script in a closure -- and hands it the prop model's own
in-memory state, so a saved parlay is watched the moment it is saved and deleting it there
deletes it. The section reads `liveparlays/parlays.json` (parlays every device sees) and
ESPN's public scoreboard and box scores in the browser. `liveparlays/index.html` is a
redirect. `nflbets/build/smoke.js` boots the built page in jsdom against both sites'
published data and walks every tab; `nflbets/build/smoke_live.js` does the same for the
Live Parlays section against a stubbed parlay file and scoreboard.

The parlays are one document for every device. `nflbets/build/sync.js`, inlined before the
prop model's script, defines the `window.storage` the model saves through and the
`window.LIVE_IO` the section's `live_parlays_v1` key goes through. On load it reads
`nflbets/sync.json` for the store's address, pulls the document (`GET <url>/doc.json`) and
lays its keys -- `parlay`, `saved`, `stake`, `bookPrice`, `margin`, and the section's
`lines` and `removed` -- over the browser's copy; every save pushes the same keys back
(`PUT <url>.json`, the document as a JSON string under a random `rev`), and a poll every
eight seconds while the page is on screen reads `<url>/rev.json` and pulls the document when
the tag moved, then redraws the builder, the open game, the section and any open Pick'ems
game. Nothing is pushed until the document has been read once, so a device that could not
reach the store never replaces it with an empty builder. A browser's first read of the
document joins rather than yields: its own saved parlays are added to the document by id,
its builder stands in for an empty one, its corrected lines and deletions are kept where
the document has none, and the result is pushed, so parlays saved before the store had an
address are not lost to whichever device seeded it. The browser then remembers the rev it
took or wrote under `nflsync_v1`, and from then on the document wins outright, since a
parlay it lacks is one another device deleted. The store is a Firebase Realtime
Database over its REST interface; with `sync.json` blank the page runs on the browser alone
and the header stamp says so. `nflbets/build/smoke.js` runs two, then more, devices against
one stubbed store and checks that each sees what the others did.

## Props: how it fits together

```
nflverse CSVs + the-odds-api  ->  weekly.py  ->  data/payload.json
part1.html + payload + part2.js + part3.js  ->  assemble.py  ->  app/prop_model_2026.html (gitignored)
                                            ->  audit.js  (must be clean, or no commit)
part1.html + part2.js + part3.js            ->  nflbets/build/build.js  ->  nflbets/index.html
```

`payload.json` is injected as `const PAY = {...}` at the top of the app, so every
number the page shows is already in the file when it loads. Its keys: `sched`
(272 games), `players` (473, each with seeded EWMA stat history), `toff`/`tdef`/
`tdefg` (team offence, defence, defence-by-position), `mkt` + `mkt_meta` (real
main lines per week), `prices` (raw odds rows per week), `stats` (actual results
per week), `corr` (the 329-entry pair correlation table), `model`/`grid`/`dist`/
`prior`/`pts`/`tdrate` (fitted model pieces), `injuries`, `depth`, `baked_at`.

`S` is the live state object, built by `freshState()` in `part2.js` from `PAY`
and then updated as weeks are ingested. It holds `players`, `teams`, `defs`,
`sched`, `processed`, `actuals`, `projections` (frozen pre-game numbers), plus
the visitor's own `parlay`, `saved`, `odds`, `stake`, `margin` and `ui`. `S` is
what gets serialised to local storage -- anything that must not persist has to
live outside it.

Tabs: **Games** (`slateView` + the `gameModal` overlay), **Parlay Builder**
(`parlayBody`), **Track Record** (`trackBody`), **Weekly Update** (admin only),
**Backup**.

### Vocabulary

- **leg** -- one line on a parlay. Keyed `gid|pid|stat` (`legKey()`), plus a
  threshold `k` and a `side` (`over`/`under`). One line per player per stat.
- **main line** -- the real sportsbook over/under for a stat (`main:true`).
- **rung** -- a step on a **threshold ladder**, the "10+, 20+, 30+" style
  alternatives (`LADDER` in `part2.js`). Hidden behind a checkbox by default.
- **game bets** -- `ml` (to win) and `ats` (to cover), one team bet per game,
  `grp:'TEAM'`.
- **corr vs indep** -- `parlayProb()` returns both: `indep` is the legs
  multiplied naively, `corr` is a gaussian-copula Monte Carlo over the shipped
  pair correlation table. `corr` is the number the UI shows. It is seeded, so it
  is deterministic.
- **SGP pricing** -- `parlayDec()`. Legs sharing a game are priced as one
  same-game parlay (never longer than multiplying), legs across games multiply.
- **margin** -- the book's cut, `light`/`typical`/`heavy`, a visitor setting.
- **suggested parlays** -- two engines. `buildSuggestions()` is the week-wide
  Safe/Medium/Aggressive tiers in the Parlay Builder, each tier growing the one
  before. `gameSuggestion()` is one Medium parlay for a single game, at most
  three legs, cached per game. `shuffleSuggestion()` deals an alternative at the
  same confidence, held outside `S` so it is forgotten when you leave the game.
- **liveWeek / weekOpen** -- the first week with an unfinished game; a later week
  is not "open" and shows fixtures only, because projections need recent form.
- **locked** -- kickoff has passed; the game stops accepting picks.

### The audit is a gate, not a test suite

`weekly.py` refuses to commit when `audit.js` is not clean, so a failing check
means the site silently stops updating and the scheduled run is marked failed.
Write checks against the app's invariants, never against whatever that week's
data happens to offer.

## Betting: how it fits together

The app is developed in a separate repo (`nfl-model-lab`) and shipped here as
one HTML file. `update.js` runs that real app headlessly in jsdom, through the
same upload path the owner would use by hand, starting from the last published
`state.json`, and writes the new state with the private parts stripped (bets,
bankroll, the owner's picks). It is idempotent: already-graded games are skipped,
games whose stats are not out yet wait for the next run.

`state.json` holds `teams`/`teamsH` (power ratings), `schedule`, `processed`
(graded games), `odds` (moneylines from nflverse, so Parlay Builder works with no
fetch), `qb`, `injuries`, `depth`, `joker`, `published`.

**The Joker** (`betting/joker/`) is a separate gradient-boosted-trees model,
fitted 2019-2025, whose per-game picks ship in `joker.json` and show next to the
main model's.

Gate: the app's embedded model numbers must equal `reference_models.json` or the
publish aborts. Shipping a model change means passing the release gate in
`nfl-model-lab`, then copying both the app and `reference_models.json` here.

Tabs: AI Picks, My Picks, Parlay Builder, Records, Bet Log, Power Ratings, Data
Upload, Backup. The public viewer drops Data Upload, Records and Bet Log.

## College: the test site

`cfb/` (X College Football Bets) is the NFL betting model's idea applied to college
football, as a separate site so it can be tried without touching the others:
moneylines and spreads only, no player props. Everything is ESPN's public feeds, free.

```
cfb/
  index.html          THE PAGE. hand-written, one file; fetches state.json on load
  state.json          generated by tools/update.js: the season, rated, called and graded
  data/history.json   2014 to last season, every game with an FBS team in it (tools/history.js, once)
  data/teams.json     ESPN's team table: names, colours, logos, conference (the job refreshes it)
  data/model.json     the fitted parameters (tools/fit.js)
  tools/espn.js       the feeds: scoreboard by week, rankings, conferences
  tools/elo.js        THE MODEL: Elo with home field, a margin multiplier, preseason regression,
                      a points-per-Elo spread and a normal margin for cover chances
  tools/fit.js        grid search on the history, last season held out
  tools/update.js     the job; tools/smoke.js the gate
```

The job replays the history and this season's finals in date order, so the ratings
are current; a game still to come gets the model's chance, spread and, from ESPN,
DraftKings' line, all frozen at the last run before kickoff and kept after it, so the
record grades what the page showed. Games over before the job first ran are graded from
the replay and marked. A side against the spread is taken only when the model and the
line differ by three points or more, and never against a lower-division opponent.
The playoff picture is three thousand simulations of the rest of the season: each
conference's title game, a committee proxy (rating less a cost per loss, the polls
where they exist), the five best-placed champions plus seven at large, straight seeding,
the bracket played out. Tabs: Games, Rankings, Power Ratings, Playoff, Parlays (browser
only, no sync), Record.

`.github/workflows/cfb.yml` runs Tuesday, Friday, Saturday morning, Saturday night, Sunday
and Monday; it commits `cfb/state.json` and `cfb/data/teams.json`.

## Hockey: the NBA Hub's idea, on this repo's shape

`nhl/` (X NHL Bets) is the college site's shape (one page, one job, one state file) with the
NBA Hub's unit and look: the NHL has no weeks, so the page opens on a day strip, yesterday to
ten days out, one card per game. Moneylines, puck lines and totals; no player props. Everything
is ESPN's public feeds, free.

```
nhl/
  index.html          THE PAGE. hand-written, one file; fetches state.json on load
  state.json          generated by tools/update.js: the season, rated, called and graded
  data/history.json   2011-12 to last season, every game with its period count (tools/history.js, once)
  data/teams.json     the fixed club table (code, ESPN id, conference, division) plus names, colours, logos from the feed
  data/model.json     the fitted parameters (tools/fit.js)
  tools/espn.js       the feed: one scoreboard a day; the club table; DraftKings' three markets
  tools/elo.js        THE MODEL: Elo with home ice, back-to-back, rest, an overtime weight and a
                      goal-margin multiplier; a Poisson goals layer for the puck line and the total
  tools/fit.js        coordinate search on the history, last season held out
  tools/update.js     the job; tools/smoke.js the gate; tools/simulate.js a fabricated season
                      played through the job in a scratch folder, run first by the workflow
```

The job reads every day of the season (skipping the days already complete in the last state),
replays the history and this season's finals in date order, and gives every game still to come
the model's chance, expected margin and total, the overtime chance, and DraftKings' moneyline,
puck line and total, frozen at the last run before puck drop and kept after it, so the record
grades what the page showed. A side is taken only where the model's chance beats the book's
implied by five points; the record counts straight-up calls on every game and the sides taken
on each market. The playoff picture is two thousand simulations of the rest of the season
(overtime and its loser point from the goals layer), the league's bracket (top three a division,
two wild cards a conference), four best-of-seven rounds with home ice to the higher seed.
Tabs: Games, Standings, Power Ratings, Playoff, Parlays (browser only, no sync), Record.

`.github/workflows/nhl.yml` runs at 3am, 10am and 5pm Eastern every day; it commits
`nhl/state.json` and `nhl/data/teams.json`.

## News: the half that is not automated

`pull-week.js` refreshes scores, records and stat bars from ESPN and
TeamRankings, and drafts `data/weekN.js`. **The narrative is written by a
person.** A draft is not live until it is added to `data/weeks.js` and
`index.html`. Read `news/HANDOFF.md` before touching anything here.

## Workflows

| Workflow | When | Does |
|---|---|---|
| `props.yml` | 12x/week: 4 price pulls (Mon/Wed/Thu/Sat), 8 post-game and stats runs | `weekly.py`, commits `props/data` to `main` |
| `update.yml` | every hour (the Joker follows the lines), plus Fri/Mon/Tue mornings ET with an afternoon catch-up each, post-game and injury-report runs | betting `update.js` + `build.js` (a check) + `smoke.js`, commits `state.json` and `joker.json` |
| `news.yml` | Fri/Mon/Tue 8am ET | `run-auto.js` |
| `elo.yml` | Tue/Fri 8:40am ET | `elo/build.py`, then the nflbets smoke, commits `elo/data` |
| `cfb.yml` | 6x/week around the college weekend | `cfb/tools/update.js` + `smoke.js`, commits `cfb/state.json` |
| `nhl.yml` | 3x/day | `nhl/tools/update.js` + `smoke.js`, commits `nhl/state.json` |

Only `props.yml` spends money (`ODDS_API_KEY`, ~7 credits a game, ~112 a week,
500 free a month). The other two use free sources.

## Gotchas

- Editing a generated file is wasted work: the next scheduled run overwrites it.
  Change the source and rebuild.
- `props/app/app.js` is a build leftover and gitignored. Delete it after
  assembling.
- `betting/app/x_nfl_betting_model.html` looks generated but is **source**.
- The props audit needs `npm ci` in `props/build/` first.
- `parlayProb()` is seeded and deterministic; don't assume Monte Carlo noise
  explains a changed number.
